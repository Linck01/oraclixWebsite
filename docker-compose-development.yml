version: '3.3'
services:
  zoraclewebsitedev:
    container_name: zoracleWebsiteDev
    restart: always
    build: .
    volumes:
      - ./src:/usr/src/app
      #- /usr/src/app/node_modules
    command: npm run development
    ports:
      - "3007:3007"
